# TECH DEBT AUDIT（src/ 全量扫描）

## 审计范围与规则

- 范围：`src/` 目录（含业务代码与测试代码）。
- 关键词与模式：`TODO`、`mock`、`fake`、空函数体（如 `() => {}` / `stop() {}` / `Promise<void> {}` 空实现）、直接 `return true`（含 `=> true`）。
- 说明：以下均以 Checkbox 形式列出，便于后续逐项整改与验收。

## P0：生产代码中明确未实现（优先立即处理）

- [x] `src/main-evolution-applier.ts:119`  
  问题：`// TODO: 实现具体的修复策略`。  
  应有真实逻辑：基于 `gene` 内容执行可回滚的修复动作（如匹配触发条件、调用策略执行器、落库执行结果、失败重试与审计日志）。

- [x] `src/main-evolution-applier.ts:132`  
  问题：`// TODO: 实现具体的优化策略`。  
  应有真实逻辑：实现可量化的优化流程（基线指标采集、优化动作执行、前后对比、收益阈值判断、回退机制）。

- [x] `src/main-evolution-applier.ts:145`  
  问题：`// TODO: 实现具体的创新策略`。  
  应有真实逻辑：实现创新策略的受控试验（小流量/低 blast radius、验证脚本、风险门禁、失败自动回滚）。

- [x] `src/runtime-api.ts:721`  
  问题：`// TODO: 需要传递 agent 对象，这里简化处理`。  
  应有真实逻辑：`/api/reflection/trigger` 应构造并注入真实 agent 上下文（agent identity、权限、会话状态），并实际触发反思流程而非仅返回 `triggered`。

## P1：生产代码中疑似占位/短路逻辑（需逐条确认）

- [ ] `src/runtime-api.ts:122`  
  问题：`return http.createServer(() => {});`（禁用 API 时返回空 handler）。  
  应有真实逻辑：返回明确的“禁用态”响应（如固定 503 + 原因），或返回受控的 noop server 并记录可观测指标，避免静默吞请求。

- [ ] `src/query-expansion/local-llm-provider.ts:232`  
  问题：生产路径文件内定义 `MockLLMQueryExpansionProvider`。  
  应有真实逻辑：将 Mock 提供者迁移到测试专用模块，生产路径只暴露真实 provider 与受控 fallback。

- [ ] `src/index.ts:322,327,339,349,422,433,766`  
  问题：多处直接 `return true`，用于“跳过/成功”语义。  
  应有真实逻辑：区分“无事可做”“跳过处理”“真正成功”“降级成功”状态码，避免布尔值掩盖处理分支，减少队列/重试误判。

- [ ] `src/group-queue.ts:286`  
  问题：写 IPC 文件成功后直接 `return true`。  
  应有真实逻辑：除写入成功外应校验落盘一致性（原子写入确认、目标文件可见性、必要时 fsync 或二次校验）并返回结构化结果。

- [ ] `src/config.ts:533`  
  问题：`isCommandAllowed` 通过前缀与禁用操作符后直接 `return true`。  
  应有真实逻辑：增加命令语义级校验（参数白名单、路径作用域、子命令限制、长度/编码检查），降低绕过风险。

- [ ] `src/collaboration-scheduler.ts:37,47`  
  问题：依赖检查函数以布尔值快速返回。  
  应有真实逻辑：返回带原因的依赖校验结果（缺失依赖列表、阻塞原因、可重试时间），便于调度器决策与可观测性。

- [ ] `src/agent-communication.ts:140`  
  问题：状态更新后直接 `return true`。  
  应有真实逻辑：返回结构化更新结果（旧状态/新状态/版本号/幂等标识），并在并发场景下保证状态一致性。

- [ ] `src/security-alerts.ts:171`  
  问题：标记事件处理完成后直接 `return true`。  
  应有真实逻辑：补充处理人、处理动作、处理证据与持久化确认，避免“仅内存标记已处理”。

- [ ] `src/keystore-audit.ts:157`  
  问题：筛选日志时条件通过直接 `return true`。  
  应有真实逻辑：当前实现可工作，但建议抽象为可审计过滤器并输出命中原因，增强审计可解释性。

- [ ] `src/sender-allowlist.ts:110`  
  问题：`allow='*'` 直接 `return true`。  
  应有真实逻辑：在全放行场景增加显式风险门禁（仅主群允许、环境开关、审计日志与告警），避免误配置导致越权触发。

- [ ] `src/mount-security.ts:218`  
  问题：路径规则校验后直接 `return true`。  
  应有真实逻辑：增加符号链接/编码绕过/平台差异路径的深度校验，并返回失败原因枚举供上层审计。

- [ ] `src/group-folder.ts:15`  
  问题：文件夹名校验通过直接 `return true`。  
  应有真实逻辑：当前规则较完整，但建议统一返回校验结果对象（是否通过 + 失败码），便于统一错误处理与安全审计。

- [ ] `src/context-engine/default-engine.ts:863`  
  问题：构造函数体为空（参数属性注入写法）。  
  应有真实逻辑：此处属于 TypeScript 合法简写，不是缺陷；仅建议在审计基线中标注为“语法型空函数，已确认可接受”。

## P2：测试代码中的 mock/fake/空实现（不等于缺陷，但可能掩盖真实集成问题）

- [ ] `src/ipc-auth.test.ts:55,60,62,64`  
  问题：空实现与 Mock 混用。  
  应有真实逻辑：补充覆盖真实 IPC 文件系统交互的集成测试，避免仅靠 stub 通过。

- [ ] `src/container-runner.test.ts:9,10,21,22,31,32,49,50,54,55,71,73,74,79,107,116,117,125,129,134,147,161,165,173,184,188,193,202`  
  问题：大量 `vi.mock` 与 `fake process`。  
  应有真实逻辑：增加真实容器生命周期 E2E（启动、输出流、超时、异常退出）验证，确保与 runtime 行为一致。

- [ ] `src/group-queue.test.ts:5,6,11,12,29,49,77,115,172,223,266,291,309,313,314,331,350,351,353,357,374,394,395,397,400,444,461,462,464,467,473,476`  
  问题：Mock FS + 多处 `return true`/空回调。  
  应有真实逻辑：补充磁盘原子写、并发队列、失败重试的端到端场景，避免单测过度理想化。

- [ ] `src/task-scheduler.test.ts:14,45,46`  
  问题：Fake timer 与空回调。  
  应有真实逻辑：增加真实时间推进或近真实调度环境测试，验证时钟漂移与任务冲突。

- [ ] `src/context-engine/__tests__/registry.test.ts:10,11,43,55,58,62,68,69,73,80,83,84,102,105,106,117,120,121`  
  问题：Mock ContextEngine 驱动测试。  
  应有真实逻辑：补充至少一组真实 engine bootstrap + 查询流程的集成测试，保证注册与默认引擎切换在真实实现中成立。

- [ ] `src/channels/telegram.test.ts:3,4,9,10,18,29,30,31,33,36,54,56,57,59,60,62,63,68,69,78,84,85,93,94,124,239,243,822,913`  
  问题：大量渠道依赖 Mock（db、env、registry、grammy、bot API）。  
  应有真实逻辑：增加 Telegram 沙箱/回放测试，覆盖消息发送、typing、媒体分支与异常路径。

- [ ] `src/container-runtime.test.ts:3,4,13,14,15,16,29,53,57,58,68,84,88,93,94,99,111,115,120,133,135,139,143`  
  问题：`execSync` 全 Mock。  
  应有真实逻辑：增加真实 runtime 探测与命令执行的契约测试（至少在 CI 可控环境跑一轮）。

- [ ] `src/formatting.test.ts:223`  
  问题：测试分支中存在直接 `return true` 短路。  
  应有真实逻辑：明确断言触发条件判断分支，而非使用早退掩盖用例缺口。

## 汇总（本轮扫描）

- 生产代码中明确 TODO：4 处（高优先级）。
- 生产代码中“空函数/Mock/直接 return true”需复核：13 处（含 1 处语法型空构造函数）。
- 测试代码中 mock/fake/空实现/直接 return true：8 组高密度区域（建议用集成/E2E补齐真实性）。
- 建议先按 P0 → P1 → P2 顺序治理，并为每项补“验收用例”后再勾选完成。
