# Telegram bot tokens are now stored per-agent in the database
# Use /add-telegram or the setup skill to add Telegram bots

# 本地 LLM 配置（可选）
# 如果设置了此变量，NanoClaw 将使用本地 LLM 进行查询扩展
# 支持的模型类型: qwen3, qwen3.5, llama3, other
# LOCAL_LLM_MODEL_PATH=/path/to/your/model.gguf
